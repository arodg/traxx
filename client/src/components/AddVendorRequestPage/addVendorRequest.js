// Table filled with all pending vendor add requests

// table columns - 
//   * vendor name
//   * requested by
// view button - goes to the same as the request vendor add screen, but the fields are all locked
// cancel button - returns user to home page