
// table filled with all pending requests if no pending some statement saying that
// table columns - 
    // * first name
    // * last name
    // * requested by
//view button - goes to the same as the request access screen, but the fields are all locked
//cancel button - returns user to add user page